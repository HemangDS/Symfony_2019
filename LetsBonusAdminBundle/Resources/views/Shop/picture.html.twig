{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}
{% block field %}
    <div>
        {% if object.image != null %}
            {% thumbnail object.image, 'default_preview' %}
        {% else %}
            <span>No picture</span>
        {% endif %}
    </div>
{% endblock %}