{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}
{% block field %}
    <div>
        {% if object.bannerimage != null %}
            {% thumbnail object.bannerimage, 'default_preview' %}
        {% else %}
            <span>No picture</span>
        {% endif %}
    </div>
{% endblock %}