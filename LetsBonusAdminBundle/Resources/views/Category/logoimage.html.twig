{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}
{% block field %}
    <div>
        {% if object.logoimage != null %}
            {% thumbnail object.logoimage, 'default_preview' %}
        {% else %}
            <span>No picture</span>
        {% endif %}
    </div>
{% endblock %}